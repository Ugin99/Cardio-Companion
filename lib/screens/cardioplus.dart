import 'package:flutter/material.dart';

class CardioPlusScreen extends StatelessWidget {
  const CardioPlusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Welcome to Cardio+ Screen!'),
      ),
    );
  }
}
